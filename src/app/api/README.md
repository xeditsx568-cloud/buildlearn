# API routes

Server HTTP endpoints. **Owner:** Programmer 2

Create subdirectories (e.g. `webhooks/clerk/`) when implementing the relevant task.

Prefer Server Actions for internal UI mutations where appropriate.
