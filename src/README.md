# Source code

Path alias: `@/` → `src/`

```
src/
  app/          Next.js routes — (marketing), (app), api/
  components/   React UI (create subdirs per phase)
  server/       Services + Prisma client
  ai/           AI pipelines (Phase 5+)
  lib/          Schemas and utilities (Phase 3+)
  env.ts        Environment validation (TASK-005)
  middleware.ts Clerk auth (TASK-101)
```

Ownership: [docs/FILE_OWNERSHIP.md](../docs/FILE_OWNERSHIP.md)
